# Build vase app and attach the artifacts to the draft release.
# Dispatched by release.yaml (a GITHUB_TOKEN push can't trigger workflows, so the
# push to the release branch won't start this on its own).

name: Release build
run-name: "Release build (v${{ inputs.version }})"

on:
  workflow_dispatch:
    inputs:
      version:
        description: Version to build (e.g. 1.2.3)
        required: true
        type: string

jobs:
  build:
    runs-on: macos-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
        with:
          ref: "release/v${{ inputs.version }}"

      - uses: dtolnay/rust-toolchain@stable
      - uses: extractions/setup-just@v3

      - name: Build universal vase.app + binary tarball
        run: just release "${{ inputs.version }}"

      - name: Upload artifacts to the draft release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release upload "v${{ inputs.version }}" \
            "dist/vase-v${{ inputs.version }}-macos.zip" \
            "dist/vase-v${{ inputs.version }}-macos.tar.gz" \
            --clobber --repo "${{ github.repository }}"
